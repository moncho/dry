box: golang:1.5.2
dev:
  steps:
    - internal/watch:
        code: |
          go build ./...
          ./source
build:
  steps:

    # golint step!
    - wercker/golint

    - script:
        name: install godep
        code: |
          go get github.com/tools/godep

    - script:
        name: godep restore
        code: |
           godep restore

    # Build the project
    - script:
        name: go build
        code: |
          go build .
    # Test the project
    - script:
        name: go test
        code: |
          go test ./...
